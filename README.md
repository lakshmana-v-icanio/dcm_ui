# DCM UI

React + TypeScript + Vite front-end for the PC Manager module.

## Prerequisites

- Node.js 18+ and npm

## Install dependencies

```bash
npm install
```

## Generate the production build

```bash
npm run build
```

This type-checks the project (`tsc -b`) and produces the optimized static build in the `dist/` folder, ready to deploy.
